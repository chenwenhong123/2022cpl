//
// Created by 2033834364 on 2022/10/21.
//
/*
 *if语句后面如果只有一个执行命令，该命令可直接写在条件后面不用加大括号；
 * for（  ；  ；  ，'''''''' ）;for指令中如果只有一个命令，那么可将指令直接打在’‘’‘’‘’处，前面用，隔开
 * 在for循环里面如果if的执行语句为continue，符合条件进入下次循环，不符合，则进行if外，for循环内的其他语句
 * 需要使用ascll码 的时候，可以不查找对应ascll值，可以直接打‘0’，‘A’，‘a’代表值
 *class-6-recursion
 *sizeof 函数，后面可以跟变量，也可以接变量类型（括号可加可不加）
 * memset( 函数名 ,int x,n)表示函数名前n个单位设为变量x
 *
 *
 */

#include <stdio.h>
#include <math.h>
#define A 10
#define B 11
#define C 12
#define D 13
#define E 14
#define F 15
